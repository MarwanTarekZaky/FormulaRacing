@model Infrastructure.DTO.LocationDTO

@{
    ViewData["Title"] = "Delete Location";
}

<h1>@ViewData["Title"]</h1>

<h4>Are you sure you want to delete this location?</h4>
<hr />


<form asp-action="DeleteConfirmed" enctype="multipart/form-data">

    <div class="form-group">
        <label asp-for="Name" class="control-label"></label>
        <input asp-for="Name" disabled class="form-control" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>
</form>

<form asp-action="DeleteConfirmed" method="post">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Delete</button>
    <a href="@Url.Action("Index", "Location")" class="btn btn-secondary">Cancel</a>
</form>
